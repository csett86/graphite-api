search_index: /tmp/graphite-render-index
time_zone: UTC
allowed_origins:
  - example.com
  - foo.example.com:8888
cache:
  type: flask_caching.backends.simplecache.SimpleCache
  default_timeout: 0
logging:
  version: 1
  disable_existing_loggers: true
  handlers:
    stdout:
      class: logging.StreamHandler
  loggers:
    graphite_render:
      handlers:
        - stdout
      propagate: true
      level: INFO
